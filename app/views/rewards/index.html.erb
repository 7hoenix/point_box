<% @rewards.each do |reward| %>
  <%= reward.name %>: <%= reward.cost %>
  <% if current_user && current_user.is_admin? %>
    <%= link_to "Edit #{reward.name}", edit_admin_reward_path(reward) %>
  <% end %>
<% end %>
