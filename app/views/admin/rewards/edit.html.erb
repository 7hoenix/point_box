<h1>Edit reward</h1>
<%= form_for [:admin, @reward], url: admin_reward_path do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :cost %>
  <%= f.select :cost, [10, 100, 1000] %>

  <%= f.submit "Change reward" %>
<% end %>
